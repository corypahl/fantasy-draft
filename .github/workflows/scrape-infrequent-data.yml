name: Scrape infrequent fantasy data

on:
  schedule:
    - cron: "37 11 * * 0"
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

jobs:
  scrape-infrequent-data:
    name: Scrape ${{ matrix.dataset }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - dataset: rookies
            key: data/rookies.json
            output: dist-data/rookies.json
          - dataset: previous-year-results
            key: data/previous-year-results.json
            output: dist-data/previous-year-results.json
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Validate workflow configuration
        env:
          AWS_GITHUB_ACTIONS_ROLE_ARN: ${{ secrets.AWS_GITHUB_ACTIONS_ROLE_ARN }}
          RANKINGS_BUCKET: ${{ secrets.RANKINGS_BUCKET }}
        run: |
          test -n "$AWS_GITHUB_ACTIONS_ROLE_ARN" || { echo "::error::Missing repository secret AWS_GITHUB_ACTIONS_ROLE_ARN"; exit 1; }
          test -n "$RANKINGS_BUCKET" || { echo "::error::Missing repository secret RANKINGS_BUCKET"; exit 1; }

      - name: Configure AWS credentials
        env:
          AWS_GITHUB_ACTIONS_ROLE_ARN: ${{ secrets.AWS_GITHUB_ACTIONS_ROLE_ARN }}
          AWS_REGION: ${{ vars.AWS_REGION || 'us-east-1' }}
        run: |
          oidc_token="$(
            curl -sSfL \
              -H "Authorization: bearer $ACTIONS_ID_TOKEN_REQUEST_TOKEN" \
              "${ACTIONS_ID_TOKEN_REQUEST_URL}&audience=sts.amazonaws.com" \
              | python3 -c 'import json, sys; print(json.load(sys.stdin)["value"])'
          )"
          read -r access_key secret_key session_token <<< "$(
            aws sts assume-role-with-web-identity \
              --role-arn "$AWS_GITHUB_ACTIONS_ROLE_ARN" \
              --role-session-name fantasy-draft-${{ matrix.dataset }} \
              --web-identity-token "$oidc_token" \
              --duration-seconds 3600 \
              --query 'Credentials.[AccessKeyId,SecretAccessKey,SessionToken]' \
              --output text
          )"
          {
            echo "AWS_ACCESS_KEY_ID=$access_key"
            echo "AWS_SECRET_ACCESS_KEY=$secret_key"
            echo "AWS_SESSION_TOKEN=$session_token"
            echo "AWS_REGION=$AWS_REGION"
            echo "AWS_DEFAULT_REGION=$AWS_REGION"
          } >> "$GITHUB_ENV"

      - name: Show Python version
        run: python3 --version

      - name: Install scraper dependencies
        run: python3 -m pip install -r scripts/requirements.txt

      - name: Scrape and upload ${{ matrix.dataset }}
        env:
          RANKINGS_BUCKET: ${{ secrets.RANKINGS_BUCKET }}
          NFL_SEASON: ${{ vars.NFL_SEASON || '2026' }}
        run: >
          python3 scripts/scrape_rankings.py
          --dataset "${{ matrix.dataset }}"
          --output "${{ matrix.output }}"
          --key "${{ matrix.key }}"
